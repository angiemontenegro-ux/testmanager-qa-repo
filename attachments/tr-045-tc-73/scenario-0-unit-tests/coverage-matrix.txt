MATRIZ DE COBERTURA — AC de SH-1056  <->  tests del dev
Fecha: 2026-09-03. Fuente: gh pr diff de VeevartBackend#1390 y VeevartFrontend#635.

| AC / item del ticket | Cubierto por test del dev | Archivos / nombres de test | Gap |
|---|---|---|---|
| AC1 crear variante "Talla" con S,M,L -> se guarda en la biblioteca global | SI | BE create-variant-type.use-case.spec + create-variant-type-option.use-case.spec + e2e "full CRUD happy path: create type, create/reorder options, list, update, delete"; FE "creates a category via the inline compose card, blocking empty and duplicate names inline", "adds comma-split values to an expanded category, skipping duplicates" | ninguno |
| AC1 "el contador aumenta" (Variantes: N / 100) | NO | no existe ninguna clave i18n de contador en packages/shop/src/products/variant-library/messages/en.json, ni componente que lo renderice | GAP REAL: el contador no se construyo. El spec.md de ambos repos declara "No size cap" / "The system MUST NOT enforce any maximum". Decision del owner posterior al ticket. |
| AC2 la biblioteca global queda disponible para elegir al crear producto | PARCIAL | BE SI: get-active-variant-library.use-case.spec + spec "Wizard fetches only active entries" + e2e "/active returns only active nested". FE NO: el picker del item wizard es la PR2 (SH-1058), fuera de esta PR. | El consumidor de UI no esta en esta PR. SH-1058 esta en estado Rejected en Jira. |
| AC2 (aclaracion 2026-07-27) la biblioteca NO se auto-instancia como set por defecto | INDIRECTO | design D2 (el producto COPIA el texto, nunca referencia el id) + D277 (multi-select de dos niveles, los valores entran al draft solo al seleccionarlos explicitamente) | Verificable solo con la PR2 del item wizard. |
| AC3 editar/eliminar una variante en uso muestra advertencia | PARCIAL | SOLO en desactivar: DeactivateCategoryDialog.test.tsx / DeactivateValueDialog.test.tsx con "deactivateDialog.usageNote" y conteo real ("count":4). En rename NO hay dialogo (edicion inline). En delete NO hay dialogo ni chequeo de uso: FE "deletes an IN-USE value directly too, with no usage check and no blocked/offer-deactivation dialog". | GAP REAL frente al AC: la advertencia existe en el camino de desactivacion, no en rename ni en delete. Decision del owner (D277). |
| AC3 los productos existentes no se rompen | SI | spec "Renaming MUST NOT retroactively change a product that already copied the prior name"; BE "hard-deletes a category with zero options even when referenced by a product", "hard-deletes an option even when referenced by a product"; design D2 copy-not-reference | ninguno. La razon de fondo: el producto copia el texto al seleccionar, no guarda el id, asi que borrar o renombrar la entrada de biblioteca no puede afectarlo. |
| Multi-tenant por organizacion (afirmado en el ticket, sin AC) | SI | BE "cross-tenant read returns nothing: tenant B never sees tenant A's category", "throws VariantTypeNotFoundError for a cross-tenant category id (never a silent empty array)", "allows the same name for a different tenant", "findByVariantTypeId and findById never cross tenants", "countUsageByLabel ... scoped to the tenant" + e2e cross-tenant isolation | ninguno |
| Validaciones (no especificadas en el ticket) | SI | FE schemas/option-category: "requires a non-empty, trimmed name", "caps the name at 80 characters (spec: Reject empty or over-length name)", "trims the name", "requires a non-empty, trimmed label"; BE "rejects a duplicate name case-insensitively, tenant-wide", "refuses a case-insensitive duplicate name for the same tenant, translated to VariantTypeNameAlreadyExistsError (23505)", "Same label allowed under different categories" | ninguno. El dev definio reglas que el ticket no pedia: no vacio, trim, tope 80, duplicado case-insensitive tenant-wide para categoria y por-categoria para opcion. |
| Reorder de opciones (no pedido en el ticket) | SI | reorder-variant-type-option.use-case.spec + variant-type-option-reorder-permutation.integration.spec (constraint DEFERRABLE, Postgres real) | alcance adicional al ticket, no un gap |
| Estado vacio | SI | FE "shows an empty state when there are no categories", "shows an empty state when the category has no values yet" + claves list.empty / list.emptyAction | ninguno |

RESUMEN DE GAPS DE COBERTURA DEL DEV: ninguno atribuible al dev.
Los dos gaps de la matriz (contador/limite 100 y advertencia en rename/delete) NO son falta de tests:
son comportamiento que el owner decidio NO construir, y los tests del dev asertan explicitamente
la conducta nueva (borrado directo sin dialogo, sin tope de tamano). El desalineado esta entre el
ticket de Jira y la decision del owner, no entre el codigo y sus tests.
