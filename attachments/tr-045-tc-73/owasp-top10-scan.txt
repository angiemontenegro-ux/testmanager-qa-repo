OWASP TOP 10:2021 — SCAN DE APLICABILIDAD
TC-73 / TR-045 · SH-1056 · VeevartBackend#1390 + VeevartFrontend#635
Fecha: 2026-09-03. Superficie: 11 endpoints REST nuevos tenant-scoped, 7 rutas proxy del portal,
2 tablas nuevas + 2 indices sobre tablas existentes, 1 pantalla nueva de Settings, 4 deps nuevas.
Referencia: OWASP Top 10:2021. Para la superficie cambiada se aplico ademas el criterio de
OWASP ASVS L1 en A01, A03 y A10.

| ID  | Riesgo                     | Aplicable | Metodo de chequeo | Veredicto | Evidencia / Justificacion |
|-----|----------------------------|-----------|-------------------|-----------|---------------------------|
| A01 | Broken Access Control      | Si  | leer el controller (guard + resolucion de tenant), leer las 7 rutas proxy, revisar IDOR en rutas anidadas, tests cross-tenant del dev | PASS | @UseGuards(ApiKeyOrCanonicalJwtGuard) a nivel de clase cubre los 11 endpoints; resolveCatalogTenantId en cada metodo con el claim del JWT ganandole al header y obligado a coincidir si llegan ambos; el portal saca tenantId de session.tenantId server-side y responde 401 sin sesion; IDOR anidado cerrado con option.variantTypeId !== variantTypeId -> 404 en 3 use cases, con tests; id de otro tenant devuelve 404 y no array vacio. Ver regression-results.txt S4, S5, S8. |
| A02 | Cryptographic Failures     | No  | -                 | N/A       | La PR no toca contrasenas, tokens, hashing, TLS ni cifrado en reposo. Los datos nuevos son nombres de categoria y labels de opcion: configuracion de catalogo, sin PII. |
| A03 | Injection                  | Si  | grep de concatenacion en SELECT/INSERT/UPDATE/DELETE; lectura de los 2 repositorios nuevos; revision del DDL de la migracion | PASS | Todo el camino de produccion usa TypeORM con parametros nombrados (:tenantId, :name, :label) u objetos where. Los unicos template literals con SQL son el DDL de la migracion (sin input de usuario) y los INSERT de fixtures del e2e. Sin queryRunner.query en codigo de aplicacion. Ver security-greps.txt S1. |
| A04 | Insecure Design            | Si  | revision del diseno nuevo: ausencia deliberada de tope de tamano, rate limiting, N+1 en el endpoint de lectura | PASS CON OBSERVACION | El spec declara explicitamente "The system MUST NOT enforce any maximum on the number of categories, or options per category" (decision confirmada por el owner), con escenario "GIVEN 200 categorias WHEN crea la 201 THEN succeeds". El N+1 de /active se detecto y cerro en la ronda 1 de review (una sola llamada batch findActiveByVariantTypeIds). Queda vivo el hecho de que /active devuelve la biblioteca completa del tenant sin paginar y sin tope, asi que el payload crece linealmente. No hay rate limiting propio de estos endpoints, pero tampoco lo habia en el patron de Categories/Locations que replican. Impacto: rendimiento y tamano de respuesta, no explotacion. Registrado, sin defecto. |
| A05 | Security Misconfiguration  | No  | -                 | N/A       | Sin cambios de infraestructura, variables de entorno, CORS, cabeceras de seguridad ni verbosidad de errores. Los archivos tocados son codigo de aplicacion, i18n y un package.json de libreria. El job "CDK governance (read-only synth)" de CI paso en verde. |
| A06 | Vulnerable and Outdated Components | Si | GitHub Advisory DB por paquete, con consulta de control para probar que el endpoint responde; consulta al registro npm por version | PASS | 4 deps nuevas (@dnd-kit/core, /sortable, /modifiers, /utilities): cero advisories en la GitHub Advisory DB. Control de validez: la misma consulta con lodash devuelve 3 advisories, asi que el cero no es un falso negativo. Los pisos declarados coinciden con las ultimas versiones publicadas (core 6.3.1, sortable 10.0.0). El package.json del backend no se toco. Observacion de higiene: los rangos son ">=" sin techo. npm audit no se pudo correr (repo no clonado, modo remote-only). |
| A07 | Identification and Authentication Failures | No | - | N/A | La PR no toca login, sesion, MFA, reset de contrasena ni emision/validacion de tokens. Reutiliza el guard existente verbatim, sin modificarlo. |
| A08 | Software and Data Integrity Failures | No | - | N/A | Sin cambios en CI/CD, sin deserializacion insegura (cero pickle/eval/YAML unsafe), sin sistema de plugins ni de updates. El DDL de la migracion no es deserializacion. Los jobs de governance de CI ("Every in-code event has a sidecar", "Reconcile catalog vs code") pasaron. |
| A09 | Security Logging and Monitoring Failures | Si | grep de Logger/logger./emitEvent/@OnEvent sobre el diff del backend | PASS CON OBSERVACION | La PR agrega 11 endpoints mutadores y CERO logging: el grep no encuentra ninguna instancia nueva de Logger ni de emision de eventos. Tampoco QUITA logging existente, asi que no es una regresion introducida por esta PR. Segun la propia PR el modulo "mirrors the Categories/Locations CRUD pattern exactly" — es decir, la ausencia de auditoria seria una caracteristica preexistente de la plataforma en Shop Settings, no algo que esta PR degrade (no verificado por mi de forma independiente contra el modulo Categories). Los datos son configuracion de catalogo tenant-scoped detras de autenticacion. Registrado como observacion, sin defecto: un gap preexistente no es FAIL de esta PR. |
| A10 | Server-Side Request Forgery (SSRF) | Si | lectura de como construyen la URL las 7 route.ts del portal | PASS CON OBSERVACION | fetch(`${getCatalogApiBaseUrl()}/v1/variant-library/types/${id}`): el host sale de configuracion del servidor, NO del cliente. Sin control de host no hay SSRF. Observacion: `id` se interpola sin encodeURIComponent y Next.js decodifica los params, asi que un %2F podria en teoria alcanzar OTRO endpoint del MISMO backend interno con los mismos headers; no sale del host, no escala privilegios y el backend exige guard + tenant por ruta. Endurecimiento sugerido: encodeURIComponent(id). Sin defecto. |

RESUMEN: 6 aplicables · 6 PASS (3 de ellos con observacion registrada) · 0 FAIL · 4 N/A
BLOQUEADORES DE SEGURIDAD: NINGUNO.
Ningun item aplicable quedo en TBD.
