OWASP TOP 10 (2021) APPLICABILITY SCAN  ·  SH-1097 / SH-1121  ·  TR-044
Target: Veevarts/VeevartFrontend @ origin/develop 60f425fa
Surface: packages/shop/src/discounts (35 files, main..develop)
Change class: UI / Frontend-only (React + TSX authoring wizard)
Reference: OWASP Top 10:2021

| ID  | Risk                       | Applicable | Check method | Verdict | Evidence / Justification |
|-----|----------------------------|------------|--------------|---------|--------------------------|
| A01 | Broken Access Control      | No  | -- | N/A | No auth, permission, profile, sharing-rule, route-guard or OAuth-scope file in the 35-file diff. The wizard authors a promotion payload; every access decision stays server-side. No IDOR surface is introduced -- the cards read ids the host already resolved and never dereference one directly. |
| A02 | Cryptographic Failures     | No  | -- | N/A | No credential, token, hashing, TLS or at-rest-encryption code in scope. Grep for password/api_key/secret/bearer/-----BEGIN over the diff returned only three prose uses of "token" in next-intl placeholder comments. |
| A03 | Injection                  | Yes | Re-ran the step-5 greps over the added lines: SQL concat, dangerouslySetInnerHTML, innerHTML, eval(, new Function(, document.write, v-html | PASS | Zero hits on every pattern. No query is constructed in this module. All admin-authored strings (rule labels, discount codes, programme names) render as React text nodes through next-intl t(...), which escapes by construction; the discount code is interpolated into the Redemption description as a message PARAMETER, not as markup. |
| A04 | Insecure Design            | Yes | Reviewed the new business logic for abuse vectors and for guards that promise more than they enforce | PASS | The two new rule-authoring guards are deliberately mirrors of existing backend guards, never new locks -- RulesLimitsStep.tsx:1626-1637 explicitly declines to invent a Ticketing lock because the backend predicate is gate-blind. Counters that nothing enforces are labelled as not-enforced rather than presented as limits (RestrictionsStep.tsx: "NEITHER IS ENFORCED"), which is the honest direction. No rate-limiting surface is introduced (no new endpoint). Client-side guards are pre-emptive UX only; the backend still rejects (ticketingAllEventsMustBeOnlyRule), so nothing security-relevant is delegated to the browser. |
| A05 | Security Misconfiguration  | No  | -- | N/A | No infra-as-code, env var, CORS, security-header, debug-toggle or error-verbosity config in the diff. (One adjacent PR-level behaviour was separately observed as already-correct: DiscountsCodesPage "shows a safe generic description instead of leaking internal error messages" -- a passing dev test, not a change in this scope.) |
| A06 | Vulnerable and Outdated Components | Yes | packages/shop/package.json + root package-lock.json delta over the baseline; GitHub Advisory DB (npm) per new package | PASS | Four new PEER deps only: @dnd-kit/core >=6.3.1, @dnd-kit/sortable >=10.0.0, @dnd-kit/modifiers >=9.0.0, @dnd-kit/utilities >=3.2.2. Advisory DB returns 0 advisories for all four. None is imported anywhere under src/discounts, so none is reachable from this story. DISCLOSED LIMIT: `npm audit` could not run -- `npm ci` fails E404 on the private @veevarts/veevart-checkout@1.11.0 (needs authenticated registry). This is an advisory-DB lookup, not a full transitive audit. |
| A07 | Identification and Authentication Failures | No | -- | N/A | No login, session, MFA, password-reset or token-issuance code in scope. One passing dev test covers the session-expiry MESSAGE only (sessionExpiredReload copy), not session handling. |
| A08 | Software and Data Integrity Failures | No | -- | N/A | No CI/CD pipeline file, no deserialization, no plugin/update system, no package-signing change in the diff. No unsafe YAML loader, pickle or eval path exists in this module. |
| A09 | Security Logging and Monitoring Failures | Yes | Grep for removed/added logging over the diff; check whether any new operation lost an audit trail | PASS | No log statement is added OR removed, so no audit trail regresses. No PII reaches a log (zero console.*/logger.* in the added lines). Observability-neutral: this change authors a payload and the server-side promotion write keeps whatever logging it already had. |
| A10 | Server-Side Request Forgery (SSRF) | No | -- | N/A | No server-side HTTP call is introduced. The wizard runs in the browser and calls the existing promotion API through the established client; no URL is built from user input, and no metadata endpoint is reachable. |

TALLY: 10 categories decided · 4 Applicable · 4 PASS · 0 FAIL · 6 N/A (each justified)
BLOCKERS: none. No Applicable category has verdict FAIL, so no security defect is owed.
