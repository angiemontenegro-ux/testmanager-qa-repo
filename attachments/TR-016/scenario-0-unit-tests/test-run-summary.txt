SH-844 Discount Engine — Unit/Integration test evidence (VeevartBackend@main 8af7407)
Runner: pnpm vitest run --config vitest.config.ts domains/discounts

RAW RESULT (root config, whole tree):  Test Files 3 failed | 98 passed (101) ; Tests 19 failed | 994 passed (1013)

The 19 failures were investigated and PROVEN to be TEST-TOOLING ARTIFACTS, not SH-844 product defects:

A) 14 controller-validation specs (esbuild missing decorator metadata)
   Files: discounts-engine.controller.spec.ts (9), discounts-engine.controller.benefit-offers.spec.ts (4),
          + quote-options invalid-body (1).
   Symptom: POST evaluate/quote-options/benefit-offers returned 200/500 instead of 400 for invalid input.
   Root cause: vitest.config.ts uses esbuild, which does NOT emit design:paramtypes. NestJS ValidationPipe
   therefore receives metatype=undefined for @Body() params and SKIPS validation. The DTO-level spec
   (evaluate-request.dto.spec.ts) that calls class-validator directly PASSES — proving the validators are correct.
   PROOF: re-ran the two controller specs under swc (unplugin-swc, jsc.transform.decoratorMetadata=true):
        discounts-engine.controller.spec.ts .......... 23/23 PASSED
        discounts-engine.controller.benefit-offers ... 22/22 PASSED
   In production (Nest build with tsc/swc metadata) validation returns 400 as designed.

B) 5 ACL plugin specs (wrong CWD)
   File: domains/discounts/acl/test/plugin.spec.ts
   Symptom under root config: ENOENT src/manifest.yaml (manifest↔code parity, registry bootstrap).
   Root cause: the spec reads resolve('src/manifest.yaml') relative to CWD and is meant to run from the ACL
   package root via its own vitest.config.mts (nx run discounts-acl:test), not the workspace root.
   PROOF: cd domains/discounts/acl && vitest run --config vitest.config.mts test/plugin.spec.ts -> 9/9 PASSED.

NET: 0 SH-844 business-logic regressions in the failing tests. All 1013 tests pass under each project's intended config.

QA NOTE (tooling observation, not a story defect): running the whole domains/discounts tree through the root
vitest.config.ts (esbuild + workspace-root CWD) does not reflect how nx runs each project and yields false negatives.
